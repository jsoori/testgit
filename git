git --version # check the version

start tracking
# git init
check status
# git status
// to stop tracking, remove the ".git" folder

// to avoid tracking some files, create a ".gitignore" file and add the desired files or folders
//addign files to the staging area

git add "filename"
//add all files to staging
git add --all

//remove files from stagign area
git reset "filename"
or to remove all
git reset


//comit changes to repo
git commit "filename"
git commit --all
git commit -m "this is a commit message to others"
//check the logs
git log

//adding a remote repo locally
git remote add origin "git hub repo url"

git remote add origin https://github.com/jsoori/testgit.git
//pushing the changes to the repo
git push -u origin master

// check remote repo
git remote -v

//get all remote branches
git branch --all

// to view the changes that you did to the code locally
git diff
